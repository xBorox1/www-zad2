html
    head
        title Quizy
    style
        include style.css
    body
        h2 Rozwiązane Quizy
        div
        each quiz in quizzes
            div(class='intr') #{quiz.intro}
                br
                button(onclick="location.href = '/results/" + quiz.id + "'") Zobacz wynik i statystyki
